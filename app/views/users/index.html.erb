
<style="position:right;"><%= form_tag users_search2_path, :html => {:method => :get} do |f| %>
   <%= text_field_tag 'search[name_contains]' %>
    <%= submit_tag "Search By Name", :class => "search-button", :name => "submit", :id => 'search_submit' %>

  <% end %></style>


<h1>All users</h1>
<%= will_paginate %>
<ul class="users">

<li><%= render @users %></li>

</ul>
<%= will_paginate %>
